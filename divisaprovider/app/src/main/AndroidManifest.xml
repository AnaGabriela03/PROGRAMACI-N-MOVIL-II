<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.divisaprovider">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permiso para leer(cliente) -->
    <permission
        android:name="com.example.divisaprovider.READ_EXCHANGE_RATE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <provider
            android:name=".ExchangeRateProvider"
            android:authorities="com.example.divisaprovider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="com.example.divisaprovider.READ_EXCHANGE_RATE" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
